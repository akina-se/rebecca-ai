/**
 * Defines the data access contract for the global dreaming module.
 * 
 * Implementations of this interface are responsible for handling persistence and
 * retrieval operations related to the dreaming process, such as user profiles and episodic buffers.
 */
export interface IGlobalDreamingRepository {
    // Add methods here
}